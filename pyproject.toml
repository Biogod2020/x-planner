[project]
name = "mission-control-core"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.27.1",
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
